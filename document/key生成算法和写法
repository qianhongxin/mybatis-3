<insert id="insert" parameterType="cn.koala.key.entity.EnterStock">
    <!--
      WARNING - @mbg.generated
    -->
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into enter_stock (order_no, in_time, related_order,
      order_type, operator, stock_status,
      key_count, remark)
    values (#{orderNo,jdbcType=CHAR}, #{inTime,jdbcType=TIMESTAMP}, #{relatedOrder,jdbcType=CHAR},
      #{orderType,jdbcType=TINYINT}, #{operator,jdbcType=VARCHAR}, #{stockStatus,jdbcType=TINYINT},
      #{keyCount,jdbcType=TINYINT}, #{remark,jdbcType=VARCHAR})
  </insert>


    <insert id="insert" parameterType="OrderItemDO" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT INTO `order_item` (
            order_id, order_no, sku_id, quantity, price,
            payment_time, delivery_time, receiver_time, closing_time,
            has_return_exchange, status, create_time, update_time, deleted
        ) VALUES (
            #{orderId}, #{orderNo}, #{skuId}, #{quantity}, #{price},
            #{paymentTime}, #{deliveryTime}, #{receiverTime}, #{closingTime},
            #{hasReturnExchange}, #{status},
            #{createTime}, #{updateTime}, #{deleted}
        )
    </insert>

    建议后者